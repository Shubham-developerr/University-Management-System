@model IEnumerable<UmsWeb.Models.Fees>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Fees Structure</h1>
<a asp-area="Admin" asp-controller="Fees" asp-action="Create" class="btn btn-primary">Create</a>
<table class="table-bordered table-striped">
    <thead>
        <tr>
            <th>Course Name</th>
            <th>Fees Per Semester</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var obj in Model)
        {
            <tr>
                <td>@obj.Course.Name</td>
                <td>@obj.CourseFee</td>
                <td>
                    <a asp-controller="Fees" asp-action="Edit" asp-route-id="@obj.Id" class="btn btn-warning">Edit</a>
                </td>
                <td>
                    <a asp-controller="Fees" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-warning">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>


